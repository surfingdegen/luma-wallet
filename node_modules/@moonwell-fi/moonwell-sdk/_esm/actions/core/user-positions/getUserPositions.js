import { getEnvironmentsFromArgs } from "../../../common/index.js";
import { getUserPositionData } from "./common.js";
export async function getUserPositions(client, args) {
    const { userAddress } = args;
    const environments = getEnvironmentsFromArgs(client, args);
    const result = await Promise.all(environments.map((environment) => getUserPositionData({
        environment,
        account: userAddress,
    })));
    return result.flat();
}
//# sourceMappingURL=getUserPositions.js.map